import { StyleSheet } from "react-native";

export default StyleSheet.create({
  headerText: {
    fontFamily: "iranian-sans",
    fontSize: 22,
  },
  bodyText: {
    fontSize: 20,
    fontFamily: "iranian-sans",
  },
});
